# 第2回レビューへの対応

## PR1: 承認境界と必須CI

着手時は4e14ed5、未コミット差分なし。出力消失、不正PLY、破損SfM、成功後の差し替えについて、修正前に承認が通ることを正規リポジトリの回帰テストで再現した。

生成後のファイル名正規化と、承認時の読み取り専用検査を分離した。成功時に成果物全体のハッシュを保存し、承認時にその集合と形式・関連インデックスを再確認する。拒否理由はreview記録に残す。人手のfail記録は成果物消失時も可能。下流が成功済みの上流再承認は従来どおり拒否する。

成功時の成果物ハッシュを持たない旧試行はそのまま承認・下流利用しない。旧ジョブを書き換えてハッシュを後付けせず、新規ジョブで実行する。これは形式検査・来歴保護であり、納品承認や画質保証ではない。

ローカル43テスト成功、skip 0。必須FFmpegランナーは抽出・doctorの実デコード2テストを実行し、skip 0。CIにFFmpeg/ffprobeを明示導入する独立ジョブを追加し、該当試験の欠落・skipを失敗にする。任意ローカル環境では従来のskip許容を維持する。過去のPhase A CIはFFmpegなしの2 skipであり、開発PCのskip 0とは異なる。

ログはローカルのwork/review-2/。この変更ではGPU再学習・ブラウザ画質・別PC納品を検証していない。

## PR2: 複数OSV・手修正マスク・内部レポート

PR1を2eb1cafとして分離した後に実装。単一OSVのinitとschema 1を残し、init-manifestでschema 2の新規ジョブを作る。詳細と再現コマンドは[複数OSVの標準手順](multi-capture.md)。

固定／移動の区別、素材ごとの採用フレーム、両魚眼の対応と個別リグ、手修正マスクのハッシュ登録・コピー・画素検査、画像一覧と赤い除外領域、素材内時刻、SfM登録状態、担当者の撮影確認記録を標準CLIへ追加した。別動画の同じフレームを同時刻扱いしない。

### 検証

- 開発PC: 全51テスト成功、skip 0。うちFFmpeg必須ランナーは10件成功、skip 0（従来2件＋複数素材8件）。
- 合成素材: 同名入力、重複ID・内容、欠損素材、レート不一致、採用範囲、固定／移動、マスク宣言・寸法・可読性・二値性、差し替え、再試行、HTMLエスケープ、旧CLIを確認。
- 実素材: M3 Max、既存5本の各frame 0・両魚眼を新規ジョブで処理。全10画像が手作業版とSHA-256一致。取り込みマスクも全10枚が元ファイルと一致。
- 実SfM: 10/10画像・5/5素材内時刻が登録され、2,781点・7,704観測。cameras.bin / images.bin / points3D.binは手作業版と全てSHA-256一致。
- 実GPU学習: 標準run trainから10,000反復を完了。エンジン報告146.728秒、最終PLYは199,952 Gaussian、形式検査合格。SHA-256は`f5064b62ce0cfe7b6a28946f166115c87360df653fdd5b81571dffe74118f272`。
- 手作業版からの学習設定差: auto_scale_posesはtrue→false（標準CLIの座標方針を維持）、save_full_checkpointはtrue→false。出力・入力参照パスも新規ジョブへ変更。学習量、実効画像サイズ、上限は同じ。生成PLYが同一という主張や、条件を統制した速度改善の主張はしない。
- 内部HTML: ブラウザで全10画像、赤いマスク重ね表示、登録状態、撮影確認欄を確認。元画像・元マスクを原寸で開くリンクも生成。
- doctor --job: 新しい複数素材ジョブの学習成功記録と最終PLY照合がPASSED。学習成功は画質承認ではない。
- 新モデルの3D描画・画質比較、別PC/LAN・ネットワーク遮断・納品: NOT_TESTED。trainの人手承認は行っていない。過去の固定5位置モデルで画質不合格だった制約を解消したとは扱わない。

ログ・manifest・比較ハッシュ・レポート・新ジョブはローカルのwork/review-2/に保存。実素材・原画像・絶対パスを含むログはGitに登録していない。

### 減った手作業と残る障害

動画別の画像フォルダ作成、元フレーム番号への改名、SfMの個別リグ指定、マスクコピーと対応検査、画像・マスク確認一覧の組立てを標準コマンドに移した。マスク自体の作成、担当者による画質・撮影不足の判断は自動化していない。原寸マスクを編集するGUI、比較ビューア、派生ジョブ、用途別QA、顧客納品パッケージは次の変更単位に残る。実寸・衝突判定・歩行・FieldSplat接続も未実装／未検証。
